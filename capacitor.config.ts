import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ru.draughtslab.corpus',
  appName: 'Draughts Corpus Engine',
  webDir: 'dist',
};

export default config;

